# robots.txt for Andrew Benyeogor's Portfolio
# https://benyeogorosenwe.com

User-agent: *
Allow: /

# Allow all bots to access the entire site
# This is a portfolio site and we want search engines to index all public content

# Specify the sitemap location
Sitemap: https://benyeogorosenwe.com/sitemap.xml

# Note: the private /admin dashboard is intentionally NOT listed here.
# Nothing on the site links to it, so crawlers won't discover it on their
# own - and listing it here would just publish the path to anyone reading
# this file. Each /admin page already carries its own
# <meta name="robots" content="noindex, nofollow"> tag (see
# src/app/admin/layout.jsx), which is what actually stops a crawler that
# does reach it from indexing it. Neither of these is a security control -
# see the TODO(auth) comments in src/app/admin/layout.jsx.